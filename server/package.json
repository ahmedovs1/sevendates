{
  "name": "sevendates-mailer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Server-side relay for the callback form. Holds the SMTP credentials so they never reach the browser.",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "verify": "node --env-file=.env verify-smtp.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "nodemailer": "^6.9.16"
  }
}
